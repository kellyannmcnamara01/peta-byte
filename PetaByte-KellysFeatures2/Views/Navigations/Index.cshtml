@model IEnumerable<PetaByte_KellysFeatures2.Models.Navigation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p class="button">
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.navTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.navUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.firstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.navTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.navUrl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.firstName)
        </td>
        <td class="crud">
            @Html.ActionLink("Edit", "Edit", new { id = item.navId }, htmlAttributes: new { @class = "button-edit" })
        </td>
        <td class="crud">
            @Html.ActionLink("Delete", "Delete", new { id = item.navId }, htmlAttributes: new { @class = "button-delete" })
        </td>
    </tr>
}

</table>
