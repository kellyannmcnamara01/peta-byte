@model PetaByte_KellysFeatures2.Models.Request

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Request</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.id)

        @Html.HiddenFor(model => model.Status)

        @Html.HiddenFor(model => model.PurposeOfVisit)

        @Html.HiddenFor(model => model.HealthCardNum)

        @Html.HiddenFor(model => model.PatientFirstName)

        @Html.HiddenFor(model => model.PatientLastName)

        @Html.HiddenFor(model => model.PatientId)

        @Html.HiddenFor(model => model.PhysicianName)

        @Html.HiddenFor(model => model.EmployeeId)

        @Html.HiddenFor(model => model.DateofAppointment)

        @Html.HiddenFor(model => model.PreferredAppTime)

        @Html.HiddenFor(model => model.PreferredDate)

        @Html.HiddenFor(model => model.DateofRequest)

        @Html.HiddenFor(model => model.DateApproved)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
